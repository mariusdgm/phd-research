experiment: "experiment_distributions"

seed: null
start_state: (1, 1)
terminal_states:
  "(8, 8)": 1.0
rows: 10 
cols: 10
p_success: 1
num_steps: 40_000
train_max_iterations: 20

# Learning hyperparameters
alpha: 0.1  # Learning rate
gamma: 0.9  # Discount factor
epsilon: 0.01  # Convergence criterion
batch_size: 32

min_samples: 10
